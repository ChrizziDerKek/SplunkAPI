apiVersion: v1
kind: Service
metadata:
  name: {{ include "splunk-api.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "splunk-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "splunk-api.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
